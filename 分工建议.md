看完现有 ABCD 分析后，我的核心判断是：

  AI 时代，三个人不应再机械分成“建模、编程、写作”。
  AI 在推公式、写代码、整理文字上通常都比单个人快，但它最容易在题意口径、数据泄漏、边界条件、单位和结果真实性上犯“看起来非常专业”的错误。

  更合适的分工是：三个人分别掌握三种不可外包的责任，每个人都可以调用 AI 完成执行工作。

  ## 推荐的三人分工

   队员                    核心责任                        必须亲自掌握                                          可大量交给 AI
  ━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   甲：问题与模型负责人    决定“我们究竟在解什么”          题意、变量、假设、目标函数、约束、公式与现实含义      候选模型搜索、推导草稿、文献线索、反例生成
  ──────────────────────  ──────────────────────────────  ────────────────────────────────────────────────────  ──────────────────────────────────────────────
   乙：计算与系统负责人    保证“结果真的算出来且能复现”    数据口径、主程序结构、输入输出、实验状态、版本管理    代码实现、调试建议、测试代码、批量实验、绘图
  ──────────────────────  ──────────────────────────────  ────────────────────────────────────────────────────  ──────────────────────────────────────────────
   丙：验证与表达负责人    判断“结果是否可信且说清楚了”    独立复算、单位/边界检查、图表证据、论文论证链         文字初稿、图表美化、摘要压缩、格式检查

  这三个角色不是流水线。每天至少做一次交叉审查：

  - 甲审乙：代码是否实现了原公式；
  - 乙审丙：论文数值是否来自最新运行结果；
  - 丙审甲：假设、边界和结论有没有证据；
  - 三人共同决定模型变更，禁止某个人和 AI 私自“升级模型”。

  一句话概括：

  > 甲管定义，乙管事实，丙管证据；AI 管高强度执行。

  ## 针对 ABC 三题的具体安排

  ### 如果选 C：最适合你们当前背景的默认方案

  现有材料已经跑通一个单日线性规划基线，且约束残差约为 (9.1\times10^{-13})，起点最稳。建议：

  - 甲负责优化模型与结算口径
    明确 SOC、充放电效率、弃电、紧急购电、调整费用、日前与滚动决策的信息集。

  - 乙负责时序数据与仿真系统
    建统一时间轴、避免未来信息泄漏，实现预测—优化—执行—结算的全年闭环。

  - 丙负责独立账单和回测
    单独写一个“不参与优化”的费用计算器，检查能量平衡、SOC 越界、紧急购电、调整事件，并形成论文图表。

  C 题中三人都要警惕：AI 很容易偷看当天实际光伏或电价，把事后最优伪装成预测策略。
  因此必须把“某一时刻允许读取哪些数据”写成程序级限制，而不只是论文里声明。

  推荐推进次序：

  1. 单日确定性模型；
  2. 连续多日 SOC 闭环；
  3. 简单因果预测基线；
  4. 第二问全年回测；
  5. 第三问滚动调整；
  6. 最后再做复杂预测或随机优化。

  不要一开始就让 AI 上 LSTM、Transformer。对这题而言，可验证的决策机制通常比模型名称更重要。

  ### 如果选 A：适合有人愿意啃数值 PDE

  建议：

  - 甲负责物理定义与守恒推导
    尤其是干基含水率、热质边界、变量系数扩散和第四问收缩坐标。

  - 乙负责有限体积/保守差分求解器
    处理圆柱中心、对流边界、隐式时间积分、阈值事件定位。

  - 丙负责收敛性、守恒和敏感性验证
    比较不同网格，核对表面通量与总含水量变化，检查 4 小时后环境和 72 小时后半径的外推。

  A 题不能让 AI 单独决定控制方程。尤其第四问中，“把半径 (R(t)) 塞进固定域方程”不等于物质守恒正确。你们三人应共同在纸上讲通：

  [
  \text{变量定义}\rightarrow\text{守恒方程}\rightarrow\text{坐标变换}
  \rightarrow\text{离散方程}\rightarrow\text{代码}
  ]

  只要其中一环没人能解释，就还不能采用。

  ### 如果选 B：只有明确的工程编程能力才建议

  建议：

  - 甲负责几何可行域、覆盖保证和停止证书；
  - 乙负责 HTTP 接口、状态机、重试、日志及模拟器联调；
  - 丙负责对抗性场景、自建仿真与正式测试管理。

  B 题后两问本质上是在线系统，不只是几何题。AI 可以快速写客户端，但不能替你们承担：

  - 请求是否幂等；
  - 超时后状态是否一致；
  - 无信号究竟排除了什么；
  - 是否真的保证发现全部目标；
  - 正式测试机会是否被错误程序消耗。

  如果三个人都没有接口联调、状态机或算法竞赛经验，我仍建议把 B 放在第三选择。

  ## 比赛期间的协作节奏

  ### 前 2–3 小时：共同选题

  三人分别独立阅读题目，然后在不看彼此结论的情况下写出：

  - 每问输入和输出；
  - 最大技术风险；
  - 预计最难验证的结论；
  - 一个六小时内能跑通的最小原型。

  同时用 AI 分别试做 A 和 C，但人必须复述 AI 方案。建议选择“六小时后最可能形成可信闭环”的题，而不是选择“AI 给出的文字最漂亮”的题。

  ### 确定题目后的第一阶段

  尽快建立唯一的“模型合同”，至少包含：

  - 符号与单位；
  - 输入数据和时间含义；
  - 目标函数；
  - 全部约束；
  - 初始和边界条件；
  - 评价指标；
  - 允许使用的信息；
  - 输出表格和图形。

  甲维护这份合同。乙的代码、丙的论文都必须引用它。

  ### 中期

  保持一条始终可运行的主线：

  原始数据 → 清洗 → 基线模型 → 结果表 → 图 → 论文数字

  复杂方法另开实验分支。若新方法没有通过统一测试，就不替换主线。比赛中最危险的不是模型简单，而是最后只剩一堆互不一致的代码、图片和文字。

  ### 最后阶段

  建议至少提前数小时冻结模型，只允许：

  - 修复确定的错误；
  - 补充验证；
  - 更新图表和文字；
  - 检查提交格式。

  不要在最后时段接受 AI 提出的“大幅升级”：它经常同时改变变量定义、参数和结论，却不会主动告诉你影响了哪些旧图表。

  ## 怎样使用 AI 才真正形成优势

  不要只问：

  > “请帮我解决 C 题。”

  应该把 AI 分成四种工作会话，尽量不让同一个上下文既当作者又当裁判：

  1. 方案生成 AI：提出模型、公式和替代方案；
  2. 代码 AI：严格按已确认合同实现；
  3. 对抗审查 AI：专门找单位、边界、泄漏、不可行和反例；
  4. 论文 AI：只允许读取已验证结果，不允许自行补数值。

  适合反复使用的审查提示词是：

  > 不要完善或赞美当前方案。请假设它是错误的，逐项寻找题意误读、单位错误、未来信息泄漏、边界条件遗漏、无法由代码复现的结论，并为每个问题给出可执行测试。

  任何 AI 生成物进入主线前，至少要满足：

  - 一名队员能不用 AI 解释它；
  - 另一名队员能用独立方法检查它；
  - 结果能由固定命令重新运行；
  - 论文中的每个关键数字能追溯到结果文件。

  ## 最终建议

  基于目前材料：

  1. 默认选 C：完成路径最稳，也最适合三位数学专业队员发挥优化、概率和验证能力。
  2. 若至少一人确实会数值 PDE，可选 A：数学主线更集中，论文推导更漂亮。
  3. 除非有人擅长在线算法和接口工程，否则不优先 B。

  最重要的观念是：不要和 AI 比写公式、代码或文字的速度；你们要负责定义问题、建立验证制度、选择可信证据并承担最终判断。 这才是 AI 能力越强时，人类队伍越需要强化的部分。

  本次实际读取了数学建模技能入口、使用指南、建模手规范、现有主分析报告及数值摘要；复制了技能要求的 使用指南.md。检查命令均正常退出（exit code 0）。未生成或修改正式建模产物，因此未触发建模终检 Subagent；竞赛当届规则与最终题面
  解释仍应以官方通知为准。